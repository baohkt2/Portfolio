version: '3.8'

services:
  app:
    image: ${APP_IMAGE:-ghcr.io/baohkt2/portfolio:latest}
    container_name: portfolio-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
